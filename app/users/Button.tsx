import React from "react";

function Button() {
  return (
    <div>
      {/* {" "}
      <li key={user.id} onClick={() => handleClick(user.id)}>
        {user.title}
      </li> */}
    </div>
  );
}

export default Button;
